import { DecodeErrorMessagePipe } from './decode-error-message.pipe';

describe('DecodeErrorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new DecodeErrorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
